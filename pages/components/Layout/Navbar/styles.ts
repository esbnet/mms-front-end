import styled from "styled-components";

export const Container = styled.div`
	ul {
		display: flex;
		flex-direction: row;
		justify-content: space-between;
		background: cyan;

		li {
			direction: none
		}
	}

`;
