import styled from "styled-components";

export const Container = styled.div`
	ul {

		display: flex;
		justify-content: space-around;
		max-width: 1200px;
		background: cyan;

		li {
			list-style: none
		}
		
	}

`;
